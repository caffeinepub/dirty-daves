# Frontend Production Environment Variables
# Copy this file to .env.production and fill in your values
# DO NOT commit .env.production to version control

# Build Metadata (Optional)
# These variables are used by the Build & Environment indicator
# If not set, the indicator will show graceful defaults

# VITE_BUILD_VERSION - Semantic version or build number (e.g., "1.0.0", "build-123")
# VITE_BUILD_VERSION=1.0.0

# VITE_BUILD_TIMESTAMP - ISO timestamp of when the build was created
# Can be set automatically in CI: VITE_BUILD_TIMESTAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
# VITE_BUILD_TIMESTAMP=2026-02-12T10:30:00Z

# Example CI usage:
# VITE_BUILD_VERSION=$CI_COMMIT_SHORT_SHA VITE_BUILD_TIMESTAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ") pnpm run build
